<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Rent" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="theme.main" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:background">@color/back_main</item>
        <item name="android:statusBarColor">@color/back_main</item>
        <item name="android:navigationBarColor">@color/back_main</item>
    </style>

    <style name="theme.publish" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:background">@color/back_main</item>
        <item name="android:statusBarColor">@color/back_main</item>
        <item name="android:navigationBarColor">@color/back_main</item>
    </style>
</resources>